`import { termHasRole } from 'dummy/helpers/term-has-role'`
`import { module, test } from 'qunit'`

module 'Unit | Helper | term has role'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = termHasRole 42
  assert.ok result
