{{#if suggestions}}
  {{#each suggestions as |suggestion index|}}
    {{suggestions-item source=suggestion.attributes.source translations=suggestion.attributes.translations
    selectSuggestion="selectSuggestion" disabled=disabled}}
  {{/each}}
{{/if}}
